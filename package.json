{
  "name": "abceed-ai-translator",
  "version": "1.2.4",
  "private": true,
  "type": "module",
  "description": "Automatically replace Japanese with AI-translated Chinese on abceed, keeping English intact.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test test/*.test.js",
    "check": "npm test && npm run build && node --check dist/abceed-ai-translator.user.js",
    "demo": "node scripts/demo.mjs"
  },
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
